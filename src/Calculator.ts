/**
 * Just an example typescript class
 */
export default class Calculator {
  /**
   * first + second
   * @param first
   * @param second
   */
  sum(first: number, second: number): number {
    return first + second;
  }
}
